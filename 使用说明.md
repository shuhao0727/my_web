# MyWeb - 开发友好的生产模式 使用说明

## 📁 简化后的文件结构
```
my_web/
├── .env                    # 唯一环境变量文件（通过注释切换）
├── docker-compose.yml      # 主配置文件（支持开发/生产双模式）
├── backend/                # 后端代码
├── frontend/              # 前端代码
└── deploy/nginx/
    ├── nginx.conf         # 生产环境配置
    └── nginx.dev.conf     # 开发环境配置（支持热重载）
```

## 🔄 模式切换方法

### **切换到开发模式**
1. **编辑 `.env` 文件**
   - 取消注释 "🔧【开发环境配置】" 部分
   - 注释 "🔧【生产环境配置】" 部分

   ```bash
   # 开发模式配置（启用）
   ENVIRONMENT=development
   DEBUG=true
   FRONTEND_MODE=development
   
   # 生产模式配置（禁用）
   # ENVIRONMENT=production
   # DEBUG=false
   # FRONTEND_MODE=production
   ```

2. **启动服务**
   ```bash
   docker-compose down && docker-compose up -d
   ```

### **切换到生产模式**
1. **编辑 `.env` 文件**
   - 注释 "🔧【开发环境配置】" 部分
   - 取消注释 "🔧【生产环境配置】" 部分

   ```bash
   # 开发模式配置（禁用）
   # ENVIRONMENT=development
   # DEBUG=true
   # FRONTEND_MODE=development
   
   # 生产模式配置（启用）
   ENVIRONMENT=production
   DEBUG=false
   FRONTEND_MODE=production
   ```

2. **启动服务**
   ```bash
   docker-compose down && docker-compose up -d
   ```

## 🚀 启动命令

### **通用启动命令**
```bash
# 开发或生产模式，取决于.env文件配置
docker-compose up -d
```

### **查看服务状态**
```bash
docker-compose ps
# 或
docker ps | grep myweb
```

### **查看日志**
```bash
# 查看所有服务日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f frontend
docker-compose logs -f backend
docker-compose logs -f nginx
```

### **停止服务**
```bash
docker-compose down
```

## ⚙️ 核心特性对比

| 特性 | 开发模式 | 生产模式 |
|------|----------|----------|
| **前端热重载** | ✅ 支持 | ❌ 不支持 |
| **源码挂载** | ✅ 挂载本地源码 | ❌ 使用编译后镜像 |
| **调试信息** | ✅ 详细日志和头部 | ❌ 简洁日志 |
| **缓存** | ❌ 禁用所有缓存 | ✅ 启用静态文件缓存 |
| **性能优化** | 调试优化 | 生产优化 |
| **安全配置** | 宽松CORS | 严格CORS |

## 🌐 访问地址

- **主网站**: http://localhost
- **前端直接访问**: http://localhost:6608
- **后端API**: http://localhost/api
- **健康检查**: http://localhost/health
- **后端直接访问**: http://localhost:8000

## 🔧 配置说明

### **.env文件关键变量**
```bash
# 模式切换
FRONTEND_MODE=development  # development 或 production

# 开发模式特有
FRONTEND_SOURCE_VOLUME=./frontend:/app
NGINX_WEBSOCKET_VOLUME=./deploy/nginx/nginx.dev.conf:/etc/nginx/nginx.conf

# 生产模式特有（不挂载源码）
FRONTEND_SOURCE_VOLUME=
NGINX_WEBSOCKET_VOLUME=
```

### **nginx配置**
- **开发环境**: `deploy/nginx/nginx.dev.conf`
  - 支持WebSocket热重载
  - 禁用缓存，便于调试
  - 详细调试头部信息
  - 宽松CORS策略

- **生产环境**: `deploy/nginx/nginx.conf`
  - 启用缓存优化
  - 简洁日志和配置
  - 严格安全策略

## 🐛 常见问题

### **1. 端口冲突**
如果80端口被占用，修改`.env`文件：
```bash
NGINX_HTTP_PORT=8080
```

### **2. 前端修改不生效（开发模式）**
确保：
1. `.env`文件中`FRONTEND_MODE=development`
2. `FRONTEND_SOURCE_VOLUME`正确配置
3. 重启服务：`docker-compose down && docker-compose up -d`

### **3. nginx配置语法错误**
测试nginx配置：
```bash
docker run --rm -v $(pwd)/deploy/nginx/nginx.dev.conf:/etc/nginx/nginx.conf:ro nginx:alpine nginx -t
```

### **4. 容器启动失败**
查看详细错误：
```bash
docker-compose logs --tail=50
```

## 📝 生产环境注意事项

1. **必须修改** `.env` 文件中的安全配置：
   - `SECRET_KEY`（至少32位随机字符串）
   - `ADMIN_PASSWORD`
   - `WANGSHU_PASSWORD`

2. **建议启用HTTPS**：
   - 配置`NGINX_HTTPS_PORT=443`
   - 添加SSL证书配置到nginx.conf

3. **定期备份**：
   - `backend/xbk.db`
   - `backend/znt.db`
   - `backend/content/` 目录

## 🔄 切换流程图
```
编辑 .env 文件
    ↓
切换开发/生产配置
    ↓
重启服务: docker-compose down && docker-compose up -d
    ↓
验证: 访问 http://localhost
```

---

**最后更新**: 2026年1月29日  
**版本**: v1.0 简化统一版